You are an expert QA Automation Engineer. Generate highly detailed software test cases for the following feature.

Feature:
{feature}

Please generate Positive, Negative, and Edge test cases.
You MUST output the response ONLY as a valid JSON object. Do NOT wrap the JSON in markdown code blocks.
The JSON object must contain a single key "test_cases" which is an array of objects.
Each test case object MUST have exactly these keys:
"id": (string) Test case ID (e.g., "TC001")
"type": (string) Must be exactly "Positive", "Negative", or "Edge"
"description": (string) Brief description of the test
"steps": (array of strings) Sequential text steps to execute
"playwright_action": (string) Raw async Python code using Playwright's 'page' object to execute these steps. You MUST use 'await' before every page action (e.g., await page.goto('...'), await page.click('...')). Combine all steps into one script snippet without markdown formatting.
"expected": (string) The expected result
