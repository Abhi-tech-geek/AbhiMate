# Runtime
Flask==3.0.0
bcrypt>=4.1,<5
groq==1.1.2
python-dotenv==1.0.1
pydantic>=2.5,<3
requests>=2.31,<3
selenium==4.21.0
webdriver-manager==4.0.1
Pillow>=10.0,<13          # visual regression (Feature #4)

# Optional backend — install if you set ABHIMATE_BACKEND=playwright.
# After install also run:  playwright install chromium
# playwright>=1.40,<2

# Dev / test (install with: pip install -r requirements-dev.txt)
